<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>All Genres</title>
  </head>
  <body>
    <h1>All Genres</h1>

    <% @genres.each do |genre| %>
      <a href="/genres/<%= genre.slug %>"><h3><%= genre.name %></h3></a>
    <% end %>
  </body>
</html>
